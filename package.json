{
  "name": "app-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "vite": "^7.0.0",
    "json-server": "^0.17.4"
  }
}
